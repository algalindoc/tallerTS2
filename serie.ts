export class Serie{

    constructor(public id:number, public name: string, public platform:string, public seasons: number, public description:string, public linkStream: string, public imagen: string)
    {

    }
}